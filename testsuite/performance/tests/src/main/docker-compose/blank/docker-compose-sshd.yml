version: "2.2"

networks:
    keycloak:
        ipam:
            config:
            - subnet: 10.0.1.0/24

services:

    sshd:
        build: sshd/centos
        image: keycloak_test_sshd:${KEYCLOAK_VERSION:-latest}
